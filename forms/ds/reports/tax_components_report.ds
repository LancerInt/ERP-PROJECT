report tax_components_report
{
    DisplayName = "Tax Components Report";
    LinkName = "tax_components_report";
    Type = "List";
    SourceFormLinkName = "tax_components";

    ListView
    {
        Columns
        {
            Column tax_type
            {
                FieldLinkName = "tax_type";
            }
            Column rate
            {
                FieldLinkName = "rate";
            }
            Column amount
            {
                FieldLinkName = "amount";
            }
        }
    }

    QuickView
    {
        Columns
        {
            Column tax_type
            {
                FieldLinkName = "tax_type";
            }
            Column rate
            {
                FieldLinkName = "rate";
            }
            Column amount
            {
                FieldLinkName = "amount";
            }
        }
    }

    DetailView
    {
        Sections
        {
            Section main
            {
                DisplayName = "Main";
                Columns
                {
                    Column tax_type
                    {
                        FieldLinkName = "tax_type";
                    }
                    Column rate
                    {
                        FieldLinkName = "rate";
                    }
                    Column amount
                    {
                        FieldLinkName = "amount";
                    }
                }
            }
        }
    }
}

form reminder_sent_flags
{
	displayname = "Reminder Sent Flags"
	success message = "Reminder Sent Flags added successfully"

	Section
	(
		type = section
		row = 1
		column = 0
		width = medium
	)

	must have reminder_date
	(
		type = date
		displayname = "Reminder Date"
		row = 1
		column = 1
		width = medium
	)

	reminder_method
	(
		type = picklist
		displayname = "Reminder Method"
		values = {"Email","Call","SMS"}
		maxchar = 100
		row = 1
		column = 1
		width = medium
	)

	notes
	(
		type = textarea
		displayname = "Notes"
		height = 100px
		row = 1
		column = 1
		width = medium
	)

	actions
	{
		on add
		{
			submit
			(
				type = submit
				displayname = "Submit"
			)
			reset
			(
				type = reset
				displayname = "Reset"
			)
		}
		on edit
		{
			update
			(
				type = submit
				displayname = "Update"
			)
			cancel
			(
				type = cancel
				displayname = "Cancel"
			)
		}
	}
}
